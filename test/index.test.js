const { AnimalConstructor } = require('../lib/index');

describe('AnimalConstructor', () => {
  it('Does what?', () => {
    const expected = 

    const input = 

    const result = 

    expect(result).toEqual(expected);
  });
});
