class Colors {
  constructor() {
    this.favorites = [];
  }

  addColor(color) {
    this.favorites.color = color;
  }

  // hasColor(color) {
    // this.favorites.color = color;
  // }
}

module.exports = Colors;